
js_day24 : i changed folder name according to sla portal day number

Task
🧩 Task 1 — useMemo (Sorting Numbers)

👉 Create:

array of random numbers

👉 Task:

sort the array (ascending) using loop or built-in

use useMemo

👉 Add:

button → generate new array

button → toggle theme

👉 Condition:

sorting should run only when array changes

🧩 Task 2 — useMemo (Search Filter)
👉 Create:

list of 5000 names

👉 Task:

filter names based on search input

use useMemo

👉 Add:

input field → search

button → toggle theme

👉 Condition:

filtering should NOT run on theme change

🧩 Task 3 — useCallback (Event Handler Optimization)
👉 Create:

parent + child component

👉 Task:

pass a function to child

function should calculate square of a number with loop

👉 Use:

useCallback

wrap child with React.memo

👉 Add:

button → change theme

👉 Condition:

child should NOT re-render on theme change

